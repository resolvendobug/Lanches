@model Lanche

    <div class="col-sm4 col-md-4 col-lg-4">
        <p><img src="~/images/@Model.ImagemUrl" alt="" class="img-fluid"></p>
        <h3>Preço: @Model.Preco.ToString("c")</h3>
        <h4><a asp-controller="Lanche" asp-action="Details" asp-route-Id="@Model.Id"> @Model.Nome</a></h4>
        <p>@Model.DescricaoCurta</p>
        <div>
            <p class="button">
                <a asp-controller="CarrinhoCompra" asp-action="AdicionarItemNoCarrinhoCompra" asp-route-Id="@Model.Id" class="btn btn-success btn-sm">Adicionar ao Carrinho</a>
            </p>
        </div>

    </div>
