@model LancheListViewModel
<div>
    <h2>@Model.CategoriaAtual</h2>
</div>

<div class="container">
    <div class="card-group">
@foreach (var lanche in Model.Lanches)
{
    <partial name="_LanchesResumo" for="@lanche" />
    
    @* <div class="card">
        <img class="card-img-top" src="~/images/@lanche.ImagemUrl" alt="" class="img-fluid">
        <div class="card-body">
        <h3 class="card-title">Preço: @lanche.Preco.ToString("c")</h3>
        <h4 class="card-text">@lanche.Nome</h4>
        <p class="card-text">@lanche.DescricaoCurta</p>
        </div>
    </div> *@

}
</div>
</div>