@model IEnumerable<Lanches.Models.Lanche>
<div>
    <h2>Todos os Lanches</h2>
</div>

<div class="container">
    <div class="row">
@foreach (var lanche in Model)
{
    <div class="col-sm4 col-md-4 col-lg-4">
        <p><img src="~/images/@lanche.ImagemUrl" alt="" class="img-fluid"></p>
        <h3>Preço: @lanche.Preco.ToString("c")</h3>
        <h4>@lanche.Nome</h4>
        <p>@lanche.DescricaoCurta</p>
    </div>
    
}
</div>
</div>